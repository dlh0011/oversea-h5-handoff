# 幸运霸主

Figma file key：`AkCiae1udy4wWFKNfw11yv`。
头图：`头图替换模块 / 286:4474`；榜单：`282:3426`。

运行：在此目录执行 `python3 -m http.server 4173`，打开 `http://127.0.0.1:4173/standalone.html`。
默认态展示新头图；点击 Leaderboard 或访问 `#leaderboard` 切换榜单。

设计宽度402px；默认高度903px，榜单高度1367px（含吸底避让），窄屏等比缩放。
现有静态工程无 package.json，无 npm 构建步骤。

交接与资源更新方法见 `HANDOFF.md`；恢复入口为 `00_交接索引.md`。
