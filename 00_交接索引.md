# 海外 H5 交接索引

- 当前项目：`oversea-h5-handoff`
- 动态管理站：`http://127.0.0.1:4328`
- H5 预览服务：`http://127.0.0.1:4329`
- 已完成：版本导入、页面预览、标注气泡拖拽、走查对勾、Codex 反馈闭环、Apple 风格管理站 UI。
- 本次继续：增加可转发的只读交付分享页，包含真实 H5、Figma、标注和下载包。
- 现有边界：Codex 继续在代码侧修复；站点只归档、预览、反馈和交付，不伪装改写源代码。
- 已完成：分享路由 `/share/<版本ID>`、公开下载入口、“复制分享链接”入口和交接文档说明。
- 已完成：Docker/Compose 生产部署配置，数据目录持久化，管理站和 H5 预览服务可一起部署到内网服务器。
- 已完成：GitHub Pages 免费发布链路，`npm run export:pages -- <版本ID>` 生成 `site/` 静态交付快照，Actions 自动发布。
- 当前团队主流程改为局域网运行：固定电脑使用 `HOST=0.0.0.0` 启动交付站，设计同事和技术通过内网地址访问；GitHub Pages 仅作为可选只读备份。
- 已完成：当前幸运霸主版本 97 张 PNG 已使用 TinyPNG 类 128 色视觉压缩，保留尺寸、透明通道和文件路径；预览资源和下载 ZIP 已替换为压缩结果。后续可用 `npm run optimize:png:visual -- <交付目录> [颜色数]` 在打包前复用；要求严格像素无损时使用 `npm run optimize:png -- <交付目录>`。
- 已完成：上传新版本支持单个资源已内联的 `.html` / `.htm`；站点会自动封装并归档为 `standalone.html`，继续沿用现有预览、气泡标注、走查记录和分享链路。依赖旁边图片、CSS、JS 或字体时仍需上传包含完整资源的 ZIP。
- 验证：`npm run build`、`npm test` 通过；分享页、交付 ZIP、反馈 Markdown 均返回 HTTP 200。
