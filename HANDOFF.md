# 海外 H5 交付站交接

## 当前结果

- 站点目录：`oversea-h5-handoff/`
- 管理站：`http://127.0.0.1:4328`
- 预览服务：`http://127.0.0.1:4329`
- 示例项目：幸运霸主 v1 · 设计走查
- 示例数据位于 `data/`，默认不提交 Git

## 部署入口

- `Dockerfile`：构建生产镜像，包含管理站和独立预览服务。
- `docker-compose.yml`：将 `data/` 挂载为持久化卷，适合公司内网服务器部署。
- `.dockerignore`：排除 `node_modules`、构建缓存、环境文件和临时上传文件。
- `scripts/export-pages.ts`：将指定版本导出为 GitHub Pages 只读快照。
- `.github/workflows/pages.yml`：推送 `site/` 后自动发布静态交付站。

部署后同事访问 `SITE_ORIGIN`，分享页访问 `SITE_ORIGIN/share/<版本ID>`。`SITE_ORIGIN` 和 `PREVIEW_ORIGIN` 必须使用同事电脑能访问的内网地址或域名，不能继续使用 `127.0.0.1`。

GitHub Pages 版本使用 `npm run export:pages -- <版本ID>` 生成 `site/`，适合公开只读交付；它与动态管理站的数据相互独立，不会把 GitHub Pages 当作标注数据库。

## 启动

```bash
cd oversea-h5-handoff
npm install
npm run build
HANDOFF_ACCESS_CODE='至少 6 位团队访问码' npm start
```

运行服务的本机访问 `127.0.0.1:4328`、`localhost:4328` 或 `::1:4328` 会自动免登录；其他电脑通过内网地址访问时仍需输入团队访问码。

本机试用可省略 `HANDOFF_ACCESS_CODE`。部署到局域网时设置 `HOST=0.0.0.0`，并为 `HANDOFF_DATA_DIR` 指定持久磁盘目录。

## 日常工作流

1. 在 Codex 中完成 H5，构建出相对路径预览（React 项目使用 `vite build --base=./`），将预览、资源和可下载源码打成 ZIP。
2. 在站点点击“新建项目”或“上传新版本”。站点会检查入口、资源、路径越界、重复版本和敏感环境文件，并独立保存该版本。
3. 在“页面与预览”中操作 H5；切换“标注”后点击具体元素，添加问题和样式建议，点击“保存标注”。在“走查记录”中更新待修改、待复查、已通过。
4. 点击“导出走查”，把 `REVIEW.md` 和 `visual-edits.json` 交给 Codex 修改源代码；导出动作会把版本标记为“已导出给 Codex”。交付页可继续标记“Codex 修复中”，修复后上传新版本会自动进入“待复查”。
5. 前端从“交付文件”下载完整归档、源码 ZIP 或走查反馈包。

## 分享给技术

在“交付文件”页点击“复制分享链接”，把 `/share/<版本ID>` 链接发给技术即可。分享页不要求登录，技术可以：

- 切换活动首页、今日榜单、昨日榜单等页面状态并查看真实 H5；
- 打开 Figma 设计链接；
- 看到已经保存的标注气泡文字、元素定位、样式建议和走查状态；
- 下载原始交付 ZIP，或从页面内查看当前版本说明。

分享页和管理站使用同一版本归档。标注内容更新后，刷新同一个分享链接即可看到最新记录；上传新版本后应把新版本的链接发给技术。局域网部署时，分享链接使用 `SITE_ORIGIN` 配置的内网地址，因此不要把 `127.0.0.1` 链接发给其他电脑。

## 交付包清单

推荐在 ZIP 根目录放 `handoff.json`，列出每个页面/状态的 HTML 路径和画布尺寸。示例见 `examples/lucky-boss.handoff.json`。使用：

```bash
npm run pack -- <Codex交付目录> <输出ZIP> <handoff.json>
```

交付包不放 `node_modules`、`.git`、`.env`、密钥或无关截图。标注只作为 Codex 修改建议，站点不会把预览内联样式冒充成源代码改动。

## 已验证

- `npm run test`：7 项通过
- `npm run build`：TypeScript 检查和 Vite 构建通过
- 幸运霸主 ZIP：成功归档，3 个状态、103 个资源可预览
- 预览服务返回 HTTP 200，并注入隔离的标注脚本

## 2026-09-17 视觉改版

- 当前工作范围为本目录的管理站 UI：`src/App.tsx`、`src/styles.css`，视觉规则见 `DESIGN.md`。
- 已替换绿色装饰卡片、宣传区和手机外壳，采用中性底色、细分隔线、真实项目缩略图、项目状态筛选和三栏走查工作区；窄屏预览按原画布比例缩放。
- awesome-design-md 上游是 DESIGN.md 合集，无原生 SKILL.md。本机已安装明确标注来源的适配版：`/Users/liuqianshuo/.codex/skills/awesome-design-md/SKILL.md`，并通过 quick_validate.py 校验。
- 本次验证：TypeScript/Vite 构建通过；7 项现有导入与存储测试通过；浏览器检查 1440、1024、390 宽度，项目列表、状态筛选、页面切换、标注模式、交付文件页和上传弹窗可显示，390/1024 宽度无横向页面溢出。
- 验证边界：未新上传归档或保存测试标注。本次浏览器工具的 iframe 元素点击未成功执行，因此不宣称完成元素选中/保存的端到端回归；原有标注通信和存储代码未修改。
- 原始 H5 与历史项目未改动。幸运霸主规则弹窗、昵称与招募时长裁剪仍属于活动源文件的既有待办，不计为此次管理站视觉改版成果。

## 2026-09-18 走查气泡与状态确认

- 预览中的已保存标注会按元素选择器测量真实页面坐标，在 H5 画布上显示编号气泡；状态在悬停浮层和走查记录中展示，点击气泡会切换到标注模式并定位到元素。
- 预览脚本会同步 H5 内部滚动位置，气泡随对应元素移动；画布缩放时仍按原始页面宽度换算坐标。
- “走查记录”列表使用圆形对勾确认按钮：点击待处理标注会切换为“已通过”，同步减少待处理数量；再次点击可恢复为“待修改”。
- 已验证：已有两条示例标注显示气泡；气泡点击能选中对应 img.asset；走查记录对勾点击后状态、侧栏数量和底部待处理数量同步更新，再次点击可恢复；390px 窄屏下气泡保留且页面无横向溢出。
- 气泡现在位于预览画布层，不再受样机 overflow 裁剪；在标注模式下可以直接拖动编号气泡，保存标注时会一并保存气泡的页面坐标。鼠标悬停气泡会显示该条标注文字，点击仍会定位到对应元素。
- 拖拽时允许气泡暂时越过样机边缘，并限制在画布可用区域附近；键盘聚焦气泡时可用方向键微调，Shift 加方向键按 10px 移动。保存坐标属于走查数据，不会直接改写 H5 源文件。
- 隔离数据副本已验证拖拽、悬停提示、保存和刷新后坐标保持；正式示例数据未写入拖拽测试位置。

## 2026-09-18 Apple 式工作台视觉优化

- 按 `awesome-design-md` 的 Mintlify/Linear 信息层级重新整理管理站视觉：浅灰页面、白色工作面、系统字体、细分隔线、黑色主操作和蓝色焦点色。
- 统一导航、项目列表、版本栏、预览工具栏、右侧检查面板、走查记录、交付文件、上传弹窗和登录页的圆角、间距、输入框与状态色。
- 保留固定 H5 画布尺寸、预览缩放、Figma 风格编号气泡、气泡拖拽/悬停提示、走查记录对勾确认、上传与下载流程；未修改原始 H5 或项目资源。
- 验证：`npm run build` 通过，`npm test` 7 项通过；管理站已重新加载最新 `dist` 产物，可通过 `http://127.0.0.1:4328` 查看。

## 2026-09-18 Apple Store 视觉参考优化

- 使用 `apple-design` 技能并通过 agent-reach 的 Jina Reader 读取 Apple Store 页面，将半透明导航材质、大留白、圆润工作单元、黑色主动作和蓝色链接层级转译到交付站。
- 项目库改为更清晰的项目单元与胶囊筛选；项目详情采用半透明版本栏、圆角预览工作区和直接按压反馈；预览、标注、走查记录和交付文件的功能结构保持不变。
- 桌面 Chrome 实际打开 `http://127.0.0.1:4328/?project=6c4f4fd6-b915-4d25-9269-d64e9b7fd967&version=afc7ac21-7a87-4985-983f-e7e5edcaa9b3` 检查通过；`npm run build`、`npm test`（7 项）和 Prettier 检查通过。
- 只修改管理站样式与设计说明，未修改幸运霸主 H5、历史交付包或标注数据。

## 2026-09-18 标注水玻璃样式

- 编号气泡改为 32px 半透明磨砂玻璃、高光边缘与柔和阴影；深色编号配状态圆点，悬停文字浮层使用同类材质。
- 调整气泡边缘留白与 300px 提示浮层的右侧定位；保留原有拖拽、点击定位、保存流程。无背景模糊支持、减少透明度或高对比度设置下使用实色底，减少动态效果时关闭气泡缩放。
- 验证：构建通过；Playwright 在 1440px/390px 检查截图、悬停浮层、拖拽与点击选中，高对比度和减少动态效果降级通过，无页面脚本错误。测试拖拽已放弃，没有保存到项目数据。

## 2026-09-18 液态玻璃气泡细化

- 根据用户提供的 Apple 实机参考，将悬停说明从偏白的浮层改为更透明的冷色液态玻璃：能透出 H5 背景、保留强亮边、内部高光和柔和深度阴影。
- 编号定位点继续保留圆形 Figma 语义，移除右下角状态小圆点以保持液态玻璃外观干净；说明浮层改为 24px 圆角并在手机宽度下保持完整可见。
- Playwright 实测：1440px 与 390px 截图中可看到预览背景透过气泡；拖拽位移、点击选中、悬停浮层、减少透明度/高对比度降级均通过，页面无脚本错误。

## 2026-09-18 Codex 反馈闭环

- `Version.workflowStatus` 兼容旧目录，默认按“正在走查”显示；状态可在交付页按“走查 → 导出反馈 → Codex 修复 → 上传待复查”推进。
- `PATCH /api/versions/:id/workflow` 使用版本 `revision` 乐观锁，避免多人同时操作覆盖状态。
- 版本对比功能已移除：实际工作流不要求把线上测试版本重新导入交付站，避免增加无效操作。
- 未修改原始 H5、历史交付包和已有标注数据。
- 验证：`npm run build`、`npm test`（7 项）通过。
